// Hacer una función que reciba la cantidad de horas trabajadas por una persona, y el valor de cada hora, y devuelva el salario correspondiente.

const sueldo = (horas, valorHora) => {
    const salario = horas * valorHora;
    console.log(salario);
}

sueldo(160, 2000);




